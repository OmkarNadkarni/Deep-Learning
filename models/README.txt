The requirements.txt contains all dependencies required to run the script
use pip install -r requirements.txt

each script is named as name_of_model_dataset_used and each file will train 2 models 
one with original dataset and one with data augmentation.
